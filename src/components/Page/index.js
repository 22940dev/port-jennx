// import { css, cx } from 'emotion';
// import React, { HTMLProps, SFC } from 'react';
// import { buildPageSpacing, spacing } from '../../styles';
// import NavigationBar from '../NavigationBar';



// function Page (children, className, ...props) {
//   const style = css`
//     padding: ${buildPageSpacing(spacing.regular, 'TOP')} ${buildPageSpacing(spacing.deci, 'RIGHT')}
//       calc(${spacing.mega} + ${buildPageSpacing(spacing.regular, 'BOTTOM')}) ${buildPageSpacing(spacing.deci, 'RIGHT')};
//   `;

//   return (
//     <div className={cx(style, className)} {...props}>
//       {<NavigationBar />}
//       {children}
      
//     </div>
//   );
// };

// export default Page;
